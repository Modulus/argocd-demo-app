name: Tags

on:
  push:
    tags:
      - v[0-9].[0-9].[0-9]
  pull_request:
    tags: 
      - v[0-9].[0-9].[0-9]

env:
  CARGO_TERM_COLOR: always


jobs:
  test:
    runs-on: ubuntu-latest
    container: rust:1.51-buster
    steps:
    - uses: actions/checkout@v2
    - uses: actions/cache@v2
      with:
        path: |
          ~/.cargo/registry
          ~/.cargo/git
          target
        key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
    - name: Run tests
      run: rustup default nightly && cargo test --verbose
  package:
    if:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: login
        uses: docker/login-action@v1
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.CR_PAT }}
      - name: Docker build app
        uses: docker-build-push-action@2
        with:
          context: .
          push: true
          tags: ghcr.io/modulus/argocd-demo-app:${{ env.GITHUB_REF }}
      - name: Docker build locust
        uses: docker-build-push-action@2
        with:
          context: .
          push: true
          tags: ghcr.io/modulus/argocd-demo-app-locust:${{ env.GITHUB_REF }}

      # - name: Build all  
      #   run: |
      #     docker build -t ghcr.io/modulus/argocd-demo-app:${{ env.GITHUB_REF }} .
      #     docker push ghcr.io/modulus/argocd-demo-app:${{ env.GITHUB_REF }}
      #     docker build -t ghcr.io/modulus/argocd-demo-app-locust:${{ env.GITHUB_REF }} ./locust
      #     docker push ghcr.io/modulus/argocd-demo-app-locust:${{ env.GITHUB_REF }}

